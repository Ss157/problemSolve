#include <iostream>
#include <vector>
// 가중치가 있는 유향그래프 <- 유향그래프o 가중치 그래프??
//<- 우선순위 큐를 이용한 bfs 
//<- vector를 이용해서 유향 그래프를 만들어야 함
using namespace std;

vector<int> 
void Make_Graph(int E)
{
    int u, v, w;
    for (int i = 0; i < E; i++)
    {
        cin >> u, v, w;
    }
}

int main(void)
{
    int V, E, K;
    cin >> V >> E >> K;
    Make_Graph(E);
}